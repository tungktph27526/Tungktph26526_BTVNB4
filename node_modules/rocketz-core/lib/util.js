"use strict";

module.exports = {
  toArr: function toArr(obj) {
    if (!Array.isArray(obj)) {
      obj = [obj];
    }

    return obj;
  },
  cloneArr: function cloneArr(arr) {
    return [].concat(arr);
  }
};